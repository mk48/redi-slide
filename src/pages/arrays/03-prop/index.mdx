---
title: 'Array Properties'
layout: ../../layouts/slide.astro
---

### length

Returns the number of elements in an array

```javascript
let team = ["Alice", "Bob", "Charlie"];
console.log(team.length); // 3

// Modify length
team.length = 2;
console.log(team); // ["Alice", "Bob"]
```